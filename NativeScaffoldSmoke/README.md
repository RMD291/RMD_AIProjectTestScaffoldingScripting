# Native App

Generated as a Model-View-Controller (MVC) project structure.

## Areas

- `models/`: application data and domain rules.
- `views/`: presentation and user-facing rendering.
- `controllers/`: request handling and coordination between models and views.
- `tests/`: automated tests for each layer.
- `config/`: non-secret environment and deployment configuration.
